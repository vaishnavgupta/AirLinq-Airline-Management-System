server:
  port: 5006
spring:
  application:
    name: pricing-service
  datasource:
    url: jdbc:mysql://localhost:3306/airlinq_pricing_db
    username: root
    password: VGsql@25
  jpa:
    hibernate:
      ddl-auto: update
  springdoc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger-ui.html
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true