server:
  port: 5007
spring:
  application:
    name: seat-service
  datasource:
    url: jdbc:mysql://localhost:3306/airlinq_seat_db
    username: root
    password: VGsql@25
  jpa:
    hibernate:
      ddl-auto: update
  springdoc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger-ui.html
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
