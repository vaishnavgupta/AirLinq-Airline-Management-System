server:
  port: 5001
spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/airlinq_user_db
    username: root
    password: VGsql@25
  jpa:
    hibernate:
      ddl-auto: update
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
jwt:
  secret-key: ${JWT_SECRET_KEY}
  expiration-ms: 86400000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

